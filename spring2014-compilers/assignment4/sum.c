int sum (int a, int b){

    int i,x,j, inv;
    for(i=0; i<a; i++){
        inv = a+b;
    }

    return inv;
}

#if(0)
int loop5(int a, int b, int c)
{
    int inv;
    inv = a +b;

   for(; a < b; a++)
   {
       b = inv;
       for(;a<b;a++){
        inv  = c+12;
         
       }
   }


   return inv;
}
#endif
